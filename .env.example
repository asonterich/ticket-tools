# Jira Personal Access Token
# Generate one at: your-jira-instance.example.com > Profile > Configure API
# Then set it as a persistent Windows environment variable:
#   setx JIRA_PAT "your_token_here"
# Restart your terminal after running setx.

JIRA_PAT=your_token_here
